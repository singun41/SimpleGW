function openToPopup(url) {
    let option = "width=1200, height=800";
    window.open(url, '', option);
}

function openToPopupSm(url) {
    let option = "width=770, height=520";
    window.open(url, '', option);
}